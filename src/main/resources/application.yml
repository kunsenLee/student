spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:orcl
    username: orcloper
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
